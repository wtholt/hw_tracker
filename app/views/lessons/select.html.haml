%h1 Select your Iron Yard Course
= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f|
  = f.select :lesson_id, options_for_select(@lessons.map{|lesson| [lesson.name, lesson.id]})
  %br
  = f.submit