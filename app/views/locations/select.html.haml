%h1 Select your Iron Yard location
= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f|
  = f.select :location_id, options_for_select(@locations.map{|location| [location.name]})
  %br
  = f.submit